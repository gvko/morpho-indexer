export enum LogLevel {
  TRACE = 'trace',
  DEBUG = 'debug',
  INFO = 'info',
  WARN = 'warn',
  ERROR = 'error',
  FATAL = 'fatal',
}

export enum Environment {
  test = 'test',
  local = 'local',
  dev = 'dev',
  prd = 'prd',
}

export enum Network {
  BASE = 'base',
  ETH = 'eth',
}
